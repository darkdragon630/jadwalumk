---
kind: wasmer.io/App.v0
name: jadwalumk
package: .

module:
  name: jadwalumk
  source: .
  runner: wasmer/node-static@latest

env:
  PORT: "8080"
  # ANTHROPIC_API_KEY di-set di Wasmer Dashboard → App → Settings → Environment Variables
  # JANGAN taruh API key di sini!

cli_args:
  - server.js
